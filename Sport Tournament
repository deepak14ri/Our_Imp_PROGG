#include<stdio.h>

int main(){
    long int n;
    scanf("%ld",&n);
    long long int t;
    while((scanf("%lld",&t)==1))
    printf("%lld\n",t-1);
    return 0;
}
'''
input:
2(testcase)
6
7
output
5
6
